# Controlador autonomo (Mundo #2) — dependencias de Python
# El paquete `controller` y `vehicle` los provee Webots (no se instalan con pip).
numpy>=1.24
opencv-python>=4.8

# Runtime de inferencia TFLite (basta uno; el controlador prueba en orden):
ai-edge-litert>=1.0    ; platform_machine != "armv7l"
# tflite-runtime         # alternativa ligera
# tensorflow>=2.15       # alternativa pesada (incluye tf.lite)

# Confirmacion opcional de peatones con HOG + SVM (Act. 3.1):
scikit-image>=0.21
joblib>=1.3
scikit-learn>=1.3
